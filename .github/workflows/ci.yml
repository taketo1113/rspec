name: RSpec Monorepo CI (Simplified)

on:
  push:
    branches:
      - 'dev'
      - 'main'
      - '*-maintenance'
  pull_request:
    branches:
      - '*'

permissions:
  contents: read

jobs:
  docs:
    uses: rspec/rspec/.github/workflows/documentation_coverage.yml@add-simplified-ci

  core:
    uses: rspec/rspec/.github/workflows/rspec.yml@add-simplified-ci
    with:
      library: 'rspec-core'
      rspec_version: '= 3.14.0.pre'

  mocks:
    uses: rspec/rspec/.github/workflows/rspec.yml@add-simplified-ci
    with:
      library: 'rspec-mocks'
      rspec_version: '= 3.14.0.pre'

  expectations:
    uses: rspec/rspec/.github/workflows/rspec.yml@add-simplified-ci
    with:
      library: 'rspec-expectations'
      rspec_version: '= 3.14.0.pre'

  support:
    uses: rspec/rspec/.github/workflows/rspec.yml@add-simplified-ci
    with:
      library: 'rspec-support'
      rspec_version: '= 3.14.0.pre'
